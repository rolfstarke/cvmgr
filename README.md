# onemanstreasure
a repo for training computer vision models in jupyter

```
bash git clone https://github.com/rolfstarke/onemanstreasure.git
```

#probably don't need to create those
#mkdir ~/onemanstreasure/models/
#mkdir ~/onemanstreasure/datasets/

ln -s /home/jupyter-admin/shared/datasets/ ~/onemanstreasure/
ln -s /home/jupyter-admin/shared/models/ ~/onemanstreasure/